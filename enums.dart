//The possible status of registrationfor a user in a session 
enum RegistrationStatus { 
  confirmed, //The user registeres but is waiting for confirmation 
  pending, //Registration have been accepted
  cancelled //Registration have been cancelled 
  }
